DATABASE_URL="file:./dev.db"
JWT_SECRET="zingte-dev-secret-change-in-production"

# Frontend URL (used for CORS + email verification links when proxied)
FRONTEND_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# This backend's public URL (Railway assigns this in production)
BACKEND_URL="http://localhost:4000"
PORT=4000

STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Email (SMTP) — leave empty for dev mode (links print in terminal)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@yourdomain.com"
EMAIL_FROM_NAME="Zingte API Hub"

CRON_SECRET="change-me-in-production"
